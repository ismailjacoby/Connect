package com.ismailjacoby.connectbackend.model.enums;

public enum UserStatus {
    ACTIVE,           // User is active and can access the platform
    BANNED,           // User is banned and cannot access the platform
    DELETED           // User account is soft-deleted
}
